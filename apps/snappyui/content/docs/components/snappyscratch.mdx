---
title: Scratch
description: The ScratchToReveal component adds an interactive scratch-off effect to reveal hidden content.
---

import Scratch from "@/components/scratch";

import { Tab, Tabs } from "fumadocs-ui/components/tabs";

---

## Demo

<Tabs
  items={[
    "Preview",
    "Code",
  ]}
>

    <Tab value="Preview">

      <div className="flex flex-col items-center justify-center mb-6  rounded-lg py-5 px-5">
        
        <Scratch />

      </div>

    </Tab>

    <Tab value="Code">

      ```tsx
'use client'
import ScratchToReveal from "@/components/ui/snappy-scratch";
import React from "react";

export default function Scratch() {
  const handleComplete = () => {
    // Do Something
  };
  return (
    <div>
      <ScratchToReveal
        width={250}
        height={250}
        minScratchPercentage={70}
        className="flex items-center justify-center overflow-hidden rounded-2xl border-2 bg-gray-100"
        onComplete={handleComplete}
      >
        <p className="text-9xl">
            🍑
        </p>
      </ScratchToReveal>
    </div>
  );
}


      ```

    </Tab>

</Tabs>

---

## Installation

<Tabs items={["pnpm", "npm", "bun", "yarn"]}>
  <Tab value="pnpm">
    ```bash 
    pnpm dlx shadcn@latest add http://snappyui.in/api/registry/scratch
    ```
  </Tab>
  <Tab value="npm">
    ```bash 
    npx shadcn@latest add http://snappyui.in/api/registry/scratch 
    ```
  </Tab>
  <Tab value="yarn">
    ```bash 
    npx shadcn@latest add http://snappyui.in/api/registry/scratch 
    ```
  </Tab>
  <Tab value="bun">
    ```bash 
    bunx --bun shadcn@latest add http://snappyui.in/api/registry/scratch 
    ```
  </Tab>
</Tabs>


---

## Props

| <b>Name</b>   | <b>Type</b>                                                                                      | <b>Default</b> | <b>Description</b>            |
| ------------- | ------------------------------------------------------------------------------------------------ | -------------- | ----------------------------- |
| **className**     | string                                                                                           | -              | The class name to apply to the component.            |
| **width**  | number                                                                                        | -              | Width of the scratch container.           |
| **height**   | number | "default"      | Width of the scratch container.         |
| **minScratchPercentage**    | number                                                                                          | false          | 	Minimum percentage of scratched area to be considered as completed (Value between 0 and 100).             |
| **children** | node                                                                                           | -              | The content to display in the marquee. |
| **onCompete** | function                                                                                           | -              | Callback function called when scratch is completed |