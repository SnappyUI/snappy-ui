---
title: Accordion
description: A flexible and responsive accordion component designed for seamless modern UI experiences.

---

import { Accordion } from "@/components/ui/snappy-accordion";

import { Tab, Tabs } from "fumadocs-ui/components/tabs";

---

## Basic Usage

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <div className="flex flex-col items-center justify-center mb-6 rounded-lg py-5 px-5">
      <Accordion title="Accordion Title" variant="default">
        This is the accordion content.
      </Accordion>
    </div>
  </Tab>

  <Tab value="Code">
    ```tsx
    import { Accordion } from "@/components/ui/snappy-accordion";

    <Accordion title="Accordion Title" variant="default">
      This is the accordion content.
    </Accordion>
    ```
  </Tab>
</Tabs>

---

## Variations

### Bordered

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <div className="flex flex-col items-center justify-center mb-6 rounded-lg py-5 px-5">
      <Accordion title="Accordion Title" variant="bordered">
        This is the accordion content.
      </Accordion>
    </div>
  </Tab>

  <Tab value="Code">
    ```tsx
    import { Accordion } from "@/components/ui/snappy-accordion";

    <Accordion title="Accordion Title" variant="bordered">
      This is the accordion content.
    </Accordion>
    ```
  </Tab>
</Tabs>

### Glassmorphic

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <div className="flex flex-col items-center justify-center mb-6 rounded-lg py-5 px-5">
      <Accordion title="Accordion Title" variant="glassmorphic">
        This is the accordion content.
      </Accordion>
    </div>
  </Tab>

  <Tab value="Code">
    ```tsx
    import { Accordion } from "@/components/ui/snappy-accordion";

    <Accordion title="Accordion Title" variant="glassmorphic">
      This is the accordion content.
    </Accordion>
    ```
  </Tab>
</Tabs>

### Minimal

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <div className="flex flex-col items-center justify-center mb-6 rounded-lg py-5 px-5">
      <Accordion title="Accordion Title" variant="minimal">
        This is the accordion content.
      </Accordion>
    </div>
  </Tab>

  <Tab value="Code">
    ```tsx
    import { Accordion } from "@/components/ui/snappy-accordion";

    <Accordion title="Accordion Title" variant="minimal">
      This is the accordion content.
    </Accordion>
    ```
  </Tab>
</Tabs>

### Gradient

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <div className="flex flex-col items-center justify-center mb-6 rounded-lg py-5 px-5">
      <Accordion title="Accordion Title" variant="gradient">
        This is the accordion content.
      </Accordion>
    </div>
  </Tab>

  <Tab value="Code">
    ```tsx
    import { Accordion } from "@/components/ui/snappy-accordion";

    <Accordion title="Accordion Title" variant="gradient">
      This is the accordion content.
    </Accordion>
    ```
  </Tab>
</Tabs>

---

## Installation

<Tabs items={["pnpm", "npm", "bun", "yarn"]}>
  <Tab value="pnpm">
    ```bash
    pnpm dlx shadcn@latest add http://snappyui.in/api/registry/accordion
    ```
  </Tab>
  <Tab value="npm">
    ```bash
    npx shadcn@latest add http://snappyui.in/api/registry/accordion
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    npx shadcn@latest add http://snappyui.in/api/registry/accordion
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bunx --bun shadcn@latest add http://snappyui.in/api/registry/accordion
    ```
  </Tab>
</Tabs>

---

## Props

| Name          | Type                                                                                                           | Default          | Description                  |
| ------------- | -------------------------------------------------------------------------------------------------------------- | ---------------- | -----------------------------|
| **title**     | `string`                                                                                                       | `-`              | Title of the accordion       |
| **children**  | `ReactNode`                                                                                                    | `-`              | Inside of the accordion      |
| **variant**   | `"default"` \| `"bordered"` \| `"shadowed"` \| `"minimal"` \| `"glassmorphic"` \| `"gradient"` \| `"elevated"` | `"default"`      | Variants of the accordion    |
| **isOpen**    | `boolean`                                                                                                      | `false`          | Set the default state        |
| **className** | `string`                                                                                                       | `-`              | You can pass Tailwind classes|
