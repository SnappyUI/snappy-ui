---
title: Hold To Delete
description: A button component with a smooth hold-to-confirm delete animation.
---

import HoldToDelete from "@/components/ui/snappy-hold-to-delete";
import { Tab, Tabs } from "fumadocs-ui/components/tabs";

## Basic Usage

<Tabs items={["Preview", "Code"]}>
  <Tab value="Preview">
    <div className="flex flex-col items-center justify-center mb-6 rounded-lg py-5 px-5">
      <HoldToDelete />
    </div>
  </Tab>

  <Tab value="Code">
    ```tsx
    import HoldToDelete from "@/components/ui/hold-to-delete";

    <HoldToDelete />
    ```
  </Tab>
</Tabs>

---

## Installation

<Tabs items={["pnpm", "npm", "bun", "yarn"]}>
  <Tab value="pnpm">
    ```bash
    pnpm dlx shadcn@latest add http://snappyui.in/api/registry/hold-to-delete
    ```
  </Tab>
  <Tab value="npm">
    ```bash
    npx shadcn@latest add http://snappyui.in/api/registry/hold-to-delete
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn dlx shadcn@latest add http://snappyui.in/api/registry/hold-to-delete
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bunx --bun shadcn@latest add http://snappyui.in/api/registry/hold-to-delete
    ```
  </Tab>
</Tabs>

---

